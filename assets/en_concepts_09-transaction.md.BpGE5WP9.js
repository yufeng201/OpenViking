import{_ as n,o as a,c as s,ag as t}from"./chunks/framework.BY_wD62d.js";const k=JSON.parse('{"title":"Path Locks and Crash Recovery","description":"","frontmatter":{"_rawMarkdown":"# Path Locks and Crash Recovery\\n\\nOpenViking uses two simple primitives — **path locks** and **redo log** — to protect the consistency of core write operations (`rm`, `mv`, `add_resource`, `session.commit`), ensuring that VikingFS, VectorDB, and QueueManager remain consistent even when failures occur.\\n\\n## Design Philosophy\\n\\nOpenViking is a context database where FS is the source of truth and VectorDB is a derived index. A lost index can be rebuilt from source data, but lost source data is unrecoverable. Therefore:\\n\\n> **Better to miss a search result than to return a bad one.**\\n\\n## Design Principles\\n\\n1. **Write-exclusive**: Path locks ensure only one write operation can operate on a path at a time\\n2. **On by default**: All data operations automatically acquire locks; no extra configuration needed\\n3. **Lock as protection**: LockContext acquires locks on entry, releases on exit — no undo/journal/commit semantics\\n4. **Only session_memory needs crash recovery**: RedoLog re-executes memory extraction after a process crash\\n5. **Queue operations run outside locks**: SemanticQueue/EmbeddingQueue enqueue operations are idempotent and retriable\\n\\n## Architecture\\n\\n```\\nService Layer (rm / mv / add_resource / session.commit)\\n    |\\n    v\\n+--[LockContext async context manager]--+\\n|                                       |\\n|  1. Create LockHandle                 |\\n|  2. Acquire path lock (poll+timeout)  |\\n|  3. Execute operations (FS+VectorDB)  |\\n|  4. Release lock                      |\\n|                                       |\\n|  On exception: auto-release lock,     |\\n|  exception propagates unchanged       |\\n+---------------------------------------+\\n    |\\n    v\\nStorage Layer (VikingFS, VectorDB, QueueManager)\\n```\\n\\n## Two Core Components\\n\\n### Component 1: PathLock + LockManager + LockContext (Path Lock System)\\n\\n**PathLock** implements file-based distributed locks with two lock types — POINT and SUBTREE — using fencing tokens to prevent TOCTOU races and automatic stale lock detection and cleanup.\\n\\n**LockHandle** is a lightweight lock holder token:\\n\\n```python\\n@dataclass\\nclass LockHandle:\\n    id: str          # Unique ID used to generate fencing tokens\\n    locks: list[str] # Acquired lock file paths\\n    created_at: float # Handle creation time\\n    last_active_at: float # Last successful acquire/refresh time\\n```\\n\\n**LockManager** is a global singleton managing lock lifecycle:\\n- Creates/releases LockHandles\\n- Background cleanup of leaked locks (in-process safety net)\\n- Executes RedoLog recovery on startup\\n\\n**LockContext** is an async context manager encapsulating the lock/unlock lifecycle:\\n\\n```python\\nfrom openviking.storage.transaction import LockContext, get_lock_manager\\n\\nasync with LockContext(get_lock_manager(), [path], lock_mode=\\"point\\") as handle:\\n    # Perform operations under lock protection\\n    ...\\n# Lock automatically released on exit (including exceptions)\\n```\\n\\n### Component 2: RedoLog (Crash Recovery)\\n\\nUsed only for the memory extraction phase of `session.commit`. Writes a marker before the operation, deletes it after success, and scans for leftover markers on startup to redo.\\n\\n```\\n/local/_system/redo/{task_id}/redo.json\\n```\\n\\nMemory extraction is idempotent — re-extracting from the same archive produces the same result.\\n\\n## Consistency Issues and Solutions\\n\\n### rm(uri)\\n\\n| Problem | Solution |\\n|---------|----------|\\n| Delete file first, then index -> file gone but index remains -> search returns non-existent file | **Reverse order**: delete index first, then file. Index deletion failure -> both file and index intact |\\n\\n**Locking strategy** (depends on target type):\\n- Deleting a **directory**: `lock_mode=\\"subtree\\"`, locks the directory itself\\n- Deleting a **file**: `lock_mode=\\"point\\"`, locks the file\'s parent directory\\n\\nOperation flow:\\n\\n```\\n1. Check whether target is a directory or file, choose lock mode\\n2. Acquire lock\\n3. Delete VectorDB index -> immediately invisible to search\\n4. Delete FS file\\n5. Release lock\\n```\\n\\nVectorDB deletion fails -> exception thrown, lock auto-released, file and index both intact. FS deletion fails -> VectorDB already deleted but file remains, retry is safe.\\n\\n### mv(old_uri, new_uri)\\n\\n| Problem | Solution |\\n|---------|----------|\\n| File moved to new path but index points to old path -> search returns old path (doesn\'t exist) | Copy first then update index; clean up copy on failure |\\n\\n**Locking strategy** (handled automatically via `lock_mode=\\"mv\\"`):\\n- Moving a **directory**: SUBTREE lock on both source path and destination parent\\n- Moving a **file**: POINT lock on both source\'s parent and destination parent\\n\\nOperation flow:\\n\\n```\\n1. Check whether source is a directory or file, set src_is_dir\\n2. Acquire mv lock (internally chooses SUBTREE or POINT based on src_is_dir)\\n3. Copy to new location (source still intact, safe)\\n4. If directory, remove the lock file carried over by cp into the copy\\n5. Update VectorDB URIs\\n   - Failure -> clean up copy, source and old index intact, consistent state\\n6. Delete source\\n7. Release lock\\n```\\n\\n### add_resource\\n\\n| Problem | Solution |\\n|---------|----------|\\n| File moved from temp to final directory, then crash -> file exists but never searchable | Two separate paths for first-time add vs incremental update |\\n| Resource already on disk but rm deletes it while semantic processing / vectorization is still running -> wasted work | Lifecycle SUBTREE lock held from finalization through processing completion |\\n\\n**First-time add** (target does not exist) — handled in `ResourceProcessor.process_resource` Phase 3.5:\\n\\n```\\n1. Acquire POINT lock on parent of final_uri\\n2. agfs.mv temp directory -> final location\\n3. Acquire SUBTREE lock on final_uri (inside POINT lock, eliminating race window)\\n4. Release POINT lock\\n5. Clean up temp directory\\n6. Enqueue SemanticMsg(lifecycle_lock_handle_id=...) -> DAG runs on final\\n7. DAG starts lock refresh loop (refreshes the lock token and updates handle activity every lock_expire/2 seconds)\\n8. DAG complete + all embeddings done -> release SUBTREE lock\\n```\\n\\nDuring this period, `rm` attempting to acquire a SUBTREE lock on the same path will fail with `ResourceBusyError`.\\n\\n**Incremental update** (target already exists) — temp stays in place:\\n\\n```\\n1. Acquire SUBTREE lock on target_uri (protect existing resource)\\n2. Enqueue SemanticMsg(uri=temp, target_uri=final, lifecycle_lock_handle_id=...)\\n3. DAG runs on temp, lock refresh loop active\\n4. DAG completion triggers sync_diff_callback or move_temp_to_target_callback\\n5. Callback completes -> release SUBTREE lock\\n```\\n\\nNote: DAG callbacks do NOT wrap operations in an outer lock. Each `VikingFS.rm` and `VikingFS.mv` has its own lock internally. An outer lock would conflict with these inner locks causing deadlock.\\n\\n**Server restart recovery**: SemanticMsg is persisted in QueueFS. On restart, `SemanticProcessor` detects that the `lifecycle_lock_handle_id` handle is missing from the in-memory LockManager and re-acquires a SUBTREE lock.\\n\\n### session.commit()\\n\\n| Problem | Solution |\\n|---------|----------|\\n| Messages cleared but archive not written -> conversation data lost | Phase 1 without lock (incomplete archive has no side effects) + Phase 2 with RedoLog |\\n\\nLLM calls have unpredictable latency (5s~60s+) and cannot be inside a lock-holding operation. The design splits into two phases:\\n\\n```\\nPhase 1 — Archive (no lock):\\n  1. Generate archive summary (LLM)\\n  2. Write archive (history/archive_N/messages.jsonl + summaries)\\n  3. Clear messages.jsonl\\n  4. Clear in-memory message list\\n\\nPhase 2 — Memory extraction + write (RedoLog):\\n  1. Write redo marker (archive_uri, session_uri, user identity)\\n  2. Extract memories from archived messages (LLM)\\n  3. Write current message state\\n  4. Write relations\\n  5. Directly enqueue SemanticQueue\\n  6. Delete redo marker\\n```\\n\\n**Crash recovery analysis**:\\n\\n| Crash point | State | Recovery action |\\n|------------|-------|----------------|\\n| During Phase 1 archive write | No marker | Incomplete archive; next commit scans history/ for index, unaffected |\\n| Phase 1 archive complete but messages not cleared | No marker | Archive complete + messages still present = redundant but safe |\\n| During Phase 2 memory extraction/write | Redo marker exists | On startup: redo extraction + write + enqueue from archive |\\n| Phase 2 complete | Redo marker deleted | No recovery needed |\\n\\n## LockContext\\n\\n`LockContext` is an **async** context manager that encapsulates lock acquisition and release:\\n\\n```python\\nfrom openviking.storage.transaction import LockContext, get_lock_manager\\n\\nlock_manager = get_lock_manager()\\n\\n# Point lock (write operations, semantic processing)\\nasync with LockContext(lock_manager, [path], lock_mode=\\"point\\"):\\n    # Perform operations...\\n    pass\\n\\n# Subtree lock (delete operations)\\nasync with LockContext(lock_manager, [path], lock_mode=\\"subtree\\"):\\n    # Perform operations...\\n    pass\\n\\n# MV lock (move operations)\\nasync with LockContext(lock_manager, [src], lock_mode=\\"mv\\", mv_dst_parent_path=dst):\\n    # Perform operations...\\n    pass\\n```\\n\\n**Lock modes**:\\n\\n| lock_mode | Use case | Behavior |\\n|-----------|----------|----------|\\n| `point` | Write operations, semantic processing | Lock the specified path; conflicts with any lock on the same path and any SUBTREE lock on ancestors |\\n| `subtree` | Delete operations | Lock the subtree root; conflicts with any lock on the same path, any lock on descendants, and any SUBTREE lock on ancestors |\\n| `mv` | Move operations | Directory move: SUBTREE lock on both source and destination; File move: POINT lock on source parent and destination (controlled by `src_is_dir`) |\\n\\n**Exception handling**: `__aexit__` always releases locks and does not swallow exceptions. Lock acquisition failure raises `LockAcquisitionError`.\\n\\n## Lock Types (POINT vs SUBTREE)\\n\\nThe lock mechanism uses two lock types to handle different conflict patterns:\\n\\n| | POINT on same path | SUBTREE on same path | POINT on descendant | SUBTREE on ancestor |\\n|---|---|---|---|---|\\n| **POINT** | Conflict | Conflict | — | Conflict |\\n| **SUBTREE** | Conflict | Conflict | Conflict | Conflict |\\n\\n- **POINT (P)**: Used for write and semantic-processing operations. Only locks a single directory. Blocks if any ancestor holds a SUBTREE lock.\\n- **SUBTREE (S)**: Used for rm and mv operations. Logically covers the entire subtree but only writes **one lock file** at the root. Before acquiring, scans all descendants and ancestor directories for conflicting locks.\\n\\n## Lock Mechanism\\n\\n### Lock Protocol\\n\\nLock file path: `{path}/.path.ovlock`\\n\\nLock file content (Fencing Token):\\n```\\n{handle_id}:{time_ns}:{lock_type}\\n```\\n\\nWhere `lock_type` is `P` (POINT) or `S` (SUBTREE).\\n\\n### Lock Acquisition (POINT mode)\\n\\n```\\nloop until timeout (poll interval: 200ms):\\n    1. Check target directory exists\\n    2. Check if target directory is locked by another operation\\n       - Stale lock? -> remove and retry\\n       - Active lock? -> wait\\n    3. Check all ancestor directories for SUBTREE locks\\n       - Stale lock? -> remove and retry\\n       - Active lock? -> wait\\n    4. Write POINT (P) lock file\\n    5. TOCTOU double-check: re-scan ancestors for SUBTREE locks\\n       - Conflict found: compare (timestamp, handle_id)\\n       - Later one (larger timestamp/handle_id) backs off (removes own lock) to prevent livelock\\n       - Wait and retry\\n    6. Verify lock file ownership (fencing token matches)\\n    7. Success\\n\\nTimeout (default 0 = no-wait) raises LockAcquisitionError\\n```\\n\\n### Lock Acquisition (SUBTREE mode)\\n\\n```\\nloop until timeout (poll interval: 200ms):\\n    1. Check target directory exists\\n    2. Check if target directory is locked by another operation\\n       - Stale lock? -> remove and retry\\n       - Active lock? -> wait\\n    3. Check all ancestor directories for SUBTREE locks\\n       - Stale lock? -> remove and retry\\n       - Active lock? -> wait\\n    4. Scan all descendant directories for any locks by other operations\\n       - Stale lock? -> remove and retry\\n       - Active lock? -> wait\\n    5. Write SUBTREE (S) lock file (only one file, at the root path)\\n    6. TOCTOU double-check: re-scan descendants and ancestors\\n       - Conflict found: compare (timestamp, handle_id)\\n       - Later one (larger timestamp/handle_id) backs off (removes own lock) to prevent livelock\\n       - Wait and retry\\n    7. Verify lock file ownership (fencing token matches)\\n    8. Success\\n\\nTimeout (default 0 = no-wait) raises LockAcquisitionError\\n```\\n\\n### Lock Expiry Cleanup\\n\\n**Stale lock detection**: PathLock checks the fencing token timestamp. Locks older than `lock_expire` (default 300s) are considered stale and are removed automatically during acquisition.\\n\\n**In-process cleanup**: LockManager checks active LockHandles every 60 seconds. Handles that still own lock files but have been inactive for longer than `lock_expire` are force-released.\\n\\n**Orphan locks**: Lock files left behind after a process crash are automatically removed via stale lock detection when any operation next attempts to acquire a lock on the same path.\\n\\n## Crash Recovery\\n\\n`LockManager.start()` automatically scans for leftover markers in `/local/_system/redo/` on startup:\\n\\n| Scenario | Recovery action |\\n|----------|----------------|\\n| session_memory extraction crash | Redo memory extraction + write + enqueue from archive |\\n| Crash while holding lock | Lock file remains in AGFS; stale detection auto-cleans on next acquisition (default 300s expiry) |\\n| Crash after enqueue, before worker processes | QueueFS SQLite persistence; worker auto-pulls after restart |\\n| Orphan index | Cleaned on L2 on-demand load |\\n\\n### Defense Summary\\n\\n| Failure scenario | Defense | Recovery timing |\\n|-----------------|--------|-----------------|\\n| Crash during operation | Lock auto-expires + stale detection | Next acquisition of same path lock |\\n| Crash during add_resource semantic processing | Lifecycle lock expires + SemanticProcessor re-acquires on restart | Worker restart |\\n| Crash during session.commit Phase 2 | RedoLog marker + redo | On restart |\\n| Crash after enqueue, before worker | QueueFS SQLite persistence | Worker restart |\\n| Orphan index | L2 on-demand load cleanup | When user accesses |\\n\\n## Configuration\\n\\nPath locks are enabled by default with no extra configuration needed. **The default behavior is no-wait**: if the path is locked, `LockAcquisitionError` is raised immediately. To allow wait/retry, configure the `storage.transaction` section:\\n\\n```json\\n{\\n  \\"storage\\": {\\n    \\"transaction\\": {\\n      \\"lock_timeout\\": 5.0,\\n      \\"lock_expire\\": 300.0\\n    }\\n  }\\n}\\n```\\n\\n| Parameter | Type | Description | Default |\\n|-----------|------|-------------|---------|\\n| `lock_timeout` | float | Lock acquisition timeout (seconds). `0` = fail immediately if locked (default). `> 0` = wait/retry up to this many seconds. | `0.0` |\\n| `lock_expire` | float | Lock inactivity threshold (seconds). Locks not refreshed within this window are treated as stale and reclaimed. | `300.0` |\\n\\n### QueueFS Persistence\\n\\nThe lock mechanism relies on QueueFS using the SQLite backend to ensure enqueued tasks survive process restarts. This is the default configuration and requires no manual setup.\\n\\n## Related Documentation\\n\\n- [Architecture](./01-architecture.md) - System architecture overview\\n- [Storage](./05-storage.md) - AGFS and vector store\\n- [Session Management](./08-session.md) - Session and memory management\\n- [Configuration](../guides/01-configuration.md) - Configuration reference\\n"},"headers":[],"relativePath":"en/concepts/09-transaction.md","filePath":"en/concepts/09-transaction.md","lastUpdated":1775121178000}'),i={name:"en/concepts/09-transaction.md"};function o(r,e,l,c,p,d){return a(),s("div",null,[...e[0]||(e[0]=[t(`<h1 id="path-locks-and-crash-recovery" tabindex="-1">Path Locks and Crash Recovery <a class="header-anchor" href="#path-locks-and-crash-recovery" aria-label="Permalink to &quot;Path Locks and Crash Recovery&quot;">​</a></h1><p>OpenViking uses two simple primitives — <strong>path locks</strong> and <strong>redo log</strong> — to protect the consistency of core write operations (<code>rm</code>, <code>mv</code>, <code>add_resource</code>, <code>session.commit</code>), ensuring that VikingFS, VectorDB, and QueueManager remain consistent even when failures occur.</p><h2 id="design-philosophy" tabindex="-1">Design Philosophy <a class="header-anchor" href="#design-philosophy" aria-label="Permalink to &quot;Design Philosophy&quot;">​</a></h2><p>OpenViking is a context database where FS is the source of truth and VectorDB is a derived index. A lost index can be rebuilt from source data, but lost source data is unrecoverable. Therefore:</p><blockquote><p><strong>Better to miss a search result than to return a bad one.</strong></p></blockquote><h2 id="design-principles" tabindex="-1">Design Principles <a class="header-anchor" href="#design-principles" aria-label="Permalink to &quot;Design Principles&quot;">​</a></h2><ol><li><strong>Write-exclusive</strong>: Path locks ensure only one write operation can operate on a path at a time</li><li><strong>On by default</strong>: All data operations automatically acquire locks; no extra configuration needed</li><li><strong>Lock as protection</strong>: LockContext acquires locks on entry, releases on exit — no undo/journal/commit semantics</li><li><strong>Only session_memory needs crash recovery</strong>: RedoLog re-executes memory extraction after a process crash</li><li><strong>Queue operations run outside locks</strong>: SemanticQueue/EmbeddingQueue enqueue operations are idempotent and retriable</li></ol><h2 id="architecture" tabindex="-1">Architecture <a class="header-anchor" href="#architecture" aria-label="Permalink to &quot;Architecture&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Service Layer (rm / mv / add_resource / session.commit)</span></span>
<span class="line"><span>    |</span></span>
<span class="line"><span>    v</span></span>
<span class="line"><span>+--[LockContext async context manager]--+</span></span>
<span class="line"><span>|                                       |</span></span>
<span class="line"><span>|  1. Create LockHandle                 |</span></span>
<span class="line"><span>|  2. Acquire path lock (poll+timeout)  |</span></span>
<span class="line"><span>|  3. Execute operations (FS+VectorDB)  |</span></span>
<span class="line"><span>|  4. Release lock                      |</span></span>
<span class="line"><span>|                                       |</span></span>
<span class="line"><span>|  On exception: auto-release lock,     |</span></span>
<span class="line"><span>|  exception propagates unchanged       |</span></span>
<span class="line"><span>+---------------------------------------+</span></span>
<span class="line"><span>    |</span></span>
<span class="line"><span>    v</span></span>
<span class="line"><span>Storage Layer (VikingFS, VectorDB, QueueManager)</span></span></code></pre></div><h2 id="two-core-components" tabindex="-1">Two Core Components <a class="header-anchor" href="#two-core-components" aria-label="Permalink to &quot;Two Core Components&quot;">​</a></h2><h3 id="component-1-pathlock-lockmanager-lockcontext-path-lock-system" tabindex="-1">Component 1: PathLock + LockManager + LockContext (Path Lock System) <a class="header-anchor" href="#component-1-pathlock-lockmanager-lockcontext-path-lock-system" aria-label="Permalink to &quot;Component 1: PathLock + LockManager + LockContext (Path Lock System)&quot;">​</a></h3><p><strong>PathLock</strong> implements file-based distributed locks with two lock types — POINT and SUBTREE — using fencing tokens to prevent TOCTOU races and automatic stale lock detection and cleanup.</p><p><strong>LockHandle</strong> is a lightweight lock holder token:</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">@dataclass</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> LockHandle</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">str</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          # Unique ID used to generate fencing tokens</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    locks: list[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">str</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Acquired lock file paths</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    created_at: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">float</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> # Handle creation time</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    last_active_at: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">float</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> # Last successful acquire/refresh time</span></span></code></pre></div><p><strong>LockManager</strong> is a global singleton managing lock lifecycle:</p><ul><li>Creates/releases LockHandles</li><li>Background cleanup of leaked locks (in-process safety net)</li><li>Executes RedoLog recovery on startup</li></ul><p><strong>LockContext</strong> is an async context manager encapsulating the lock/unlock lifecycle:</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> openviking.storage.transaction </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> LockContext, get_lock_manager</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> with</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> LockContext(get_lock_manager(), [path], </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">lock_mode</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;point&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> handle:</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # Perform operations under lock protection</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    ...</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Lock automatically released on exit (including exceptions)</span></span></code></pre></div><h3 id="component-2-redolog-crash-recovery" tabindex="-1">Component 2: RedoLog (Crash Recovery) <a class="header-anchor" href="#component-2-redolog-crash-recovery" aria-label="Permalink to &quot;Component 2: RedoLog (Crash Recovery)&quot;">​</a></h3><p>Used only for the memory extraction phase of <code>session.commit</code>. Writes a marker before the operation, deletes it after success, and scans for leftover markers on startup to redo.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/local/_system/redo/{task_id}/redo.json</span></span></code></pre></div><p>Memory extraction is idempotent — re-extracting from the same archive produces the same result.</p><h2 id="consistency-issues-and-solutions" tabindex="-1">Consistency Issues and Solutions <a class="header-anchor" href="#consistency-issues-and-solutions" aria-label="Permalink to &quot;Consistency Issues and Solutions&quot;">​</a></h2><h3 id="rm-uri" tabindex="-1">rm(uri) <a class="header-anchor" href="#rm-uri" aria-label="Permalink to &quot;rm(uri)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Problem</th><th>Solution</th></tr></thead><tbody><tr><td>Delete file first, then index -&gt; file gone but index remains -&gt; search returns non-existent file</td><td><strong>Reverse order</strong>: delete index first, then file. Index deletion failure -&gt; both file and index intact</td></tr></tbody></table><p><strong>Locking strategy</strong> (depends on target type):</p><ul><li>Deleting a <strong>directory</strong>: <code>lock_mode=&quot;subtree&quot;</code>, locks the directory itself</li><li>Deleting a <strong>file</strong>: <code>lock_mode=&quot;point&quot;</code>, locks the file&#39;s parent directory</li></ul><p>Operation flow:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. Check whether target is a directory or file, choose lock mode</span></span>
<span class="line"><span>2. Acquire lock</span></span>
<span class="line"><span>3. Delete VectorDB index -&gt; immediately invisible to search</span></span>
<span class="line"><span>4. Delete FS file</span></span>
<span class="line"><span>5. Release lock</span></span></code></pre></div><p>VectorDB deletion fails -&gt; exception thrown, lock auto-released, file and index both intact. FS deletion fails -&gt; VectorDB already deleted but file remains, retry is safe.</p><h3 id="mv-old-uri-new-uri" tabindex="-1">mv(old_uri, new_uri) <a class="header-anchor" href="#mv-old-uri-new-uri" aria-label="Permalink to &quot;mv(old_uri, new_uri)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Problem</th><th>Solution</th></tr></thead><tbody><tr><td>File moved to new path but index points to old path -&gt; search returns old path (doesn&#39;t exist)</td><td>Copy first then update index; clean up copy on failure</td></tr></tbody></table><p><strong>Locking strategy</strong> (handled automatically via <code>lock_mode=&quot;mv&quot;</code>):</p><ul><li>Moving a <strong>directory</strong>: SUBTREE lock on both source path and destination parent</li><li>Moving a <strong>file</strong>: POINT lock on both source&#39;s parent and destination parent</li></ul><p>Operation flow:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. Check whether source is a directory or file, set src_is_dir</span></span>
<span class="line"><span>2. Acquire mv lock (internally chooses SUBTREE or POINT based on src_is_dir)</span></span>
<span class="line"><span>3. Copy to new location (source still intact, safe)</span></span>
<span class="line"><span>4. If directory, remove the lock file carried over by cp into the copy</span></span>
<span class="line"><span>5. Update VectorDB URIs</span></span>
<span class="line"><span>   - Failure -&gt; clean up copy, source and old index intact, consistent state</span></span>
<span class="line"><span>6. Delete source</span></span>
<span class="line"><span>7. Release lock</span></span></code></pre></div><h3 id="add-resource" tabindex="-1">add_resource <a class="header-anchor" href="#add-resource" aria-label="Permalink to &quot;add_resource&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Problem</th><th>Solution</th></tr></thead><tbody><tr><td>File moved from temp to final directory, then crash -&gt; file exists but never searchable</td><td>Two separate paths for first-time add vs incremental update</td></tr><tr><td>Resource already on disk but rm deletes it while semantic processing / vectorization is still running -&gt; wasted work</td><td>Lifecycle SUBTREE lock held from finalization through processing completion</td></tr></tbody></table><p><strong>First-time add</strong> (target does not exist) — handled in <code>ResourceProcessor.process_resource</code> Phase 3.5:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. Acquire POINT lock on parent of final_uri</span></span>
<span class="line"><span>2. agfs.mv temp directory -&gt; final location</span></span>
<span class="line"><span>3. Acquire SUBTREE lock on final_uri (inside POINT lock, eliminating race window)</span></span>
<span class="line"><span>4. Release POINT lock</span></span>
<span class="line"><span>5. Clean up temp directory</span></span>
<span class="line"><span>6. Enqueue SemanticMsg(lifecycle_lock_handle_id=...) -&gt; DAG runs on final</span></span>
<span class="line"><span>7. DAG starts lock refresh loop (refreshes the lock token and updates handle activity every lock_expire/2 seconds)</span></span>
<span class="line"><span>8. DAG complete + all embeddings done -&gt; release SUBTREE lock</span></span></code></pre></div><p>During this period, <code>rm</code> attempting to acquire a SUBTREE lock on the same path will fail with <code>ResourceBusyError</code>.</p><p><strong>Incremental update</strong> (target already exists) — temp stays in place:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. Acquire SUBTREE lock on target_uri (protect existing resource)</span></span>
<span class="line"><span>2. Enqueue SemanticMsg(uri=temp, target_uri=final, lifecycle_lock_handle_id=...)</span></span>
<span class="line"><span>3. DAG runs on temp, lock refresh loop active</span></span>
<span class="line"><span>4. DAG completion triggers sync_diff_callback or move_temp_to_target_callback</span></span>
<span class="line"><span>5. Callback completes -&gt; release SUBTREE lock</span></span></code></pre></div><p>Note: DAG callbacks do NOT wrap operations in an outer lock. Each <code>VikingFS.rm</code> and <code>VikingFS.mv</code> has its own lock internally. An outer lock would conflict with these inner locks causing deadlock.</p><p><strong>Server restart recovery</strong>: SemanticMsg is persisted in QueueFS. On restart, <code>SemanticProcessor</code> detects that the <code>lifecycle_lock_handle_id</code> handle is missing from the in-memory LockManager and re-acquires a SUBTREE lock.</p><h3 id="session-commit" tabindex="-1">session.commit() <a class="header-anchor" href="#session-commit" aria-label="Permalink to &quot;session.commit()&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Problem</th><th>Solution</th></tr></thead><tbody><tr><td>Messages cleared but archive not written -&gt; conversation data lost</td><td>Phase 1 without lock (incomplete archive has no side effects) + Phase 2 with RedoLog</td></tr></tbody></table><p>LLM calls have unpredictable latency (5s~60s+) and cannot be inside a lock-holding operation. The design splits into two phases:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Phase 1 — Archive (no lock):</span></span>
<span class="line"><span>  1. Generate archive summary (LLM)</span></span>
<span class="line"><span>  2. Write archive (history/archive_N/messages.jsonl + summaries)</span></span>
<span class="line"><span>  3. Clear messages.jsonl</span></span>
<span class="line"><span>  4. Clear in-memory message list</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Phase 2 — Memory extraction + write (RedoLog):</span></span>
<span class="line"><span>  1. Write redo marker (archive_uri, session_uri, user identity)</span></span>
<span class="line"><span>  2. Extract memories from archived messages (LLM)</span></span>
<span class="line"><span>  3. Write current message state</span></span>
<span class="line"><span>  4. Write relations</span></span>
<span class="line"><span>  5. Directly enqueue SemanticQueue</span></span>
<span class="line"><span>  6. Delete redo marker</span></span></code></pre></div><p><strong>Crash recovery analysis</strong>:</p><table tabindex="0"><thead><tr><th>Crash point</th><th>State</th><th>Recovery action</th></tr></thead><tbody><tr><td>During Phase 1 archive write</td><td>No marker</td><td>Incomplete archive; next commit scans history/ for index, unaffected</td></tr><tr><td>Phase 1 archive complete but messages not cleared</td><td>No marker</td><td>Archive complete + messages still present = redundant but safe</td></tr><tr><td>During Phase 2 memory extraction/write</td><td>Redo marker exists</td><td>On startup: redo extraction + write + enqueue from archive</td></tr><tr><td>Phase 2 complete</td><td>Redo marker deleted</td><td>No recovery needed</td></tr></tbody></table><h2 id="lockcontext" tabindex="-1">LockContext <a class="header-anchor" href="#lockcontext" aria-label="Permalink to &quot;LockContext&quot;">​</a></h2><p><code>LockContext</code> is an <strong>async</strong> context manager that encapsulates lock acquisition and release:</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> openviking.storage.transaction </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> LockContext, get_lock_manager</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">lock_manager </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> get_lock_manager()</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Point lock (write operations, semantic processing)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> with</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> LockContext(lock_manager, [path], </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">lock_mode</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;point&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">):</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # Perform operations...</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    pass</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Subtree lock (delete operations)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> with</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> LockContext(lock_manager, [path], </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">lock_mode</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;subtree&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">):</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # Perform operations...</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    pass</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># MV lock (move operations)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> with</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> LockContext(lock_manager, [src], </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">lock_mode</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;mv&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">mv_dst_parent_path</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">dst):</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # Perform operations...</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    pass</span></span></code></pre></div><p><strong>Lock modes</strong>:</p><table tabindex="0"><thead><tr><th>lock_mode</th><th>Use case</th><th>Behavior</th></tr></thead><tbody><tr><td><code>point</code></td><td>Write operations, semantic processing</td><td>Lock the specified path; conflicts with any lock on the same path and any SUBTREE lock on ancestors</td></tr><tr><td><code>subtree</code></td><td>Delete operations</td><td>Lock the subtree root; conflicts with any lock on the same path, any lock on descendants, and any SUBTREE lock on ancestors</td></tr><tr><td><code>mv</code></td><td>Move operations</td><td>Directory move: SUBTREE lock on both source and destination; File move: POINT lock on source parent and destination (controlled by <code>src_is_dir</code>)</td></tr></tbody></table><p><strong>Exception handling</strong>: <code>__aexit__</code> always releases locks and does not swallow exceptions. Lock acquisition failure raises <code>LockAcquisitionError</code>.</p><h2 id="lock-types-point-vs-subtree" tabindex="-1">Lock Types (POINT vs SUBTREE) <a class="header-anchor" href="#lock-types-point-vs-subtree" aria-label="Permalink to &quot;Lock Types (POINT vs SUBTREE)&quot;">​</a></h2><p>The lock mechanism uses two lock types to handle different conflict patterns:</p><table tabindex="0"><thead><tr><th></th><th>POINT on same path</th><th>SUBTREE on same path</th><th>POINT on descendant</th><th>SUBTREE on ancestor</th></tr></thead><tbody><tr><td><strong>POINT</strong></td><td>Conflict</td><td>Conflict</td><td>—</td><td>Conflict</td></tr><tr><td><strong>SUBTREE</strong></td><td>Conflict</td><td>Conflict</td><td>Conflict</td><td>Conflict</td></tr></tbody></table><ul><li><strong>POINT (P)</strong>: Used for write and semantic-processing operations. Only locks a single directory. Blocks if any ancestor holds a SUBTREE lock.</li><li><strong>SUBTREE (S)</strong>: Used for rm and mv operations. Logically covers the entire subtree but only writes <strong>one lock file</strong> at the root. Before acquiring, scans all descendants and ancestor directories for conflicting locks.</li></ul><h2 id="lock-mechanism" tabindex="-1">Lock Mechanism <a class="header-anchor" href="#lock-mechanism" aria-label="Permalink to &quot;Lock Mechanism&quot;">​</a></h2><h3 id="lock-protocol" tabindex="-1">Lock Protocol <a class="header-anchor" href="#lock-protocol" aria-label="Permalink to &quot;Lock Protocol&quot;">​</a></h3><p>Lock file path: <code>{path}/.path.ovlock</code></p><p>Lock file content (Fencing Token):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{handle_id}:{time_ns}:{lock_type}</span></span></code></pre></div><p>Where <code>lock_type</code> is <code>P</code> (POINT) or <code>S</code> (SUBTREE).</p><h3 id="lock-acquisition-point-mode" tabindex="-1">Lock Acquisition (POINT mode) <a class="header-anchor" href="#lock-acquisition-point-mode" aria-label="Permalink to &quot;Lock Acquisition (POINT mode)&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>loop until timeout (poll interval: 200ms):</span></span>
<span class="line"><span>    1. Check target directory exists</span></span>
<span class="line"><span>    2. Check if target directory is locked by another operation</span></span>
<span class="line"><span>       - Stale lock? -&gt; remove and retry</span></span>
<span class="line"><span>       - Active lock? -&gt; wait</span></span>
<span class="line"><span>    3. Check all ancestor directories for SUBTREE locks</span></span>
<span class="line"><span>       - Stale lock? -&gt; remove and retry</span></span>
<span class="line"><span>       - Active lock? -&gt; wait</span></span>
<span class="line"><span>    4. Write POINT (P) lock file</span></span>
<span class="line"><span>    5. TOCTOU double-check: re-scan ancestors for SUBTREE locks</span></span>
<span class="line"><span>       - Conflict found: compare (timestamp, handle_id)</span></span>
<span class="line"><span>       - Later one (larger timestamp/handle_id) backs off (removes own lock) to prevent livelock</span></span>
<span class="line"><span>       - Wait and retry</span></span>
<span class="line"><span>    6. Verify lock file ownership (fencing token matches)</span></span>
<span class="line"><span>    7. Success</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Timeout (default 0 = no-wait) raises LockAcquisitionError</span></span></code></pre></div><h3 id="lock-acquisition-subtree-mode" tabindex="-1">Lock Acquisition (SUBTREE mode) <a class="header-anchor" href="#lock-acquisition-subtree-mode" aria-label="Permalink to &quot;Lock Acquisition (SUBTREE mode)&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>loop until timeout (poll interval: 200ms):</span></span>
<span class="line"><span>    1. Check target directory exists</span></span>
<span class="line"><span>    2. Check if target directory is locked by another operation</span></span>
<span class="line"><span>       - Stale lock? -&gt; remove and retry</span></span>
<span class="line"><span>       - Active lock? -&gt; wait</span></span>
<span class="line"><span>    3. Check all ancestor directories for SUBTREE locks</span></span>
<span class="line"><span>       - Stale lock? -&gt; remove and retry</span></span>
<span class="line"><span>       - Active lock? -&gt; wait</span></span>
<span class="line"><span>    4. Scan all descendant directories for any locks by other operations</span></span>
<span class="line"><span>       - Stale lock? -&gt; remove and retry</span></span>
<span class="line"><span>       - Active lock? -&gt; wait</span></span>
<span class="line"><span>    5. Write SUBTREE (S) lock file (only one file, at the root path)</span></span>
<span class="line"><span>    6. TOCTOU double-check: re-scan descendants and ancestors</span></span>
<span class="line"><span>       - Conflict found: compare (timestamp, handle_id)</span></span>
<span class="line"><span>       - Later one (larger timestamp/handle_id) backs off (removes own lock) to prevent livelock</span></span>
<span class="line"><span>       - Wait and retry</span></span>
<span class="line"><span>    7. Verify lock file ownership (fencing token matches)</span></span>
<span class="line"><span>    8. Success</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Timeout (default 0 = no-wait) raises LockAcquisitionError</span></span></code></pre></div><h3 id="lock-expiry-cleanup" tabindex="-1">Lock Expiry Cleanup <a class="header-anchor" href="#lock-expiry-cleanup" aria-label="Permalink to &quot;Lock Expiry Cleanup&quot;">​</a></h3><p><strong>Stale lock detection</strong>: PathLock checks the fencing token timestamp. Locks older than <code>lock_expire</code> (default 300s) are considered stale and are removed automatically during acquisition.</p><p><strong>In-process cleanup</strong>: LockManager checks active LockHandles every 60 seconds. Handles that still own lock files but have been inactive for longer than <code>lock_expire</code> are force-released.</p><p><strong>Orphan locks</strong>: Lock files left behind after a process crash are automatically removed via stale lock detection when any operation next attempts to acquire a lock on the same path.</p><h2 id="crash-recovery" tabindex="-1">Crash Recovery <a class="header-anchor" href="#crash-recovery" aria-label="Permalink to &quot;Crash Recovery&quot;">​</a></h2><p><code>LockManager.start()</code> automatically scans for leftover markers in <code>/local/_system/redo/</code> on startup:</p><table tabindex="0"><thead><tr><th>Scenario</th><th>Recovery action</th></tr></thead><tbody><tr><td>session_memory extraction crash</td><td>Redo memory extraction + write + enqueue from archive</td></tr><tr><td>Crash while holding lock</td><td>Lock file remains in AGFS; stale detection auto-cleans on next acquisition (default 300s expiry)</td></tr><tr><td>Crash after enqueue, before worker processes</td><td>QueueFS SQLite persistence; worker auto-pulls after restart</td></tr><tr><td>Orphan index</td><td>Cleaned on L2 on-demand load</td></tr></tbody></table><h3 id="defense-summary" tabindex="-1">Defense Summary <a class="header-anchor" href="#defense-summary" aria-label="Permalink to &quot;Defense Summary&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Failure scenario</th><th>Defense</th><th>Recovery timing</th></tr></thead><tbody><tr><td>Crash during operation</td><td>Lock auto-expires + stale detection</td><td>Next acquisition of same path lock</td></tr><tr><td>Crash during add_resource semantic processing</td><td>Lifecycle lock expires + SemanticProcessor re-acquires on restart</td><td>Worker restart</td></tr><tr><td>Crash during session.commit Phase 2</td><td>RedoLog marker + redo</td><td>On restart</td></tr><tr><td>Crash after enqueue, before worker</td><td>QueueFS SQLite persistence</td><td>Worker restart</td></tr><tr><td>Orphan index</td><td>L2 on-demand load cleanup</td><td>When user accesses</td></tr></tbody></table><h2 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h2><p>Path locks are enabled by default with no extra configuration needed. <strong>The default behavior is no-wait</strong>: if the path is locked, <code>LockAcquisitionError</code> is raised immediately. To allow wait/retry, configure the <code>storage.transaction</code> section:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;storage&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;transaction&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;lock_timeout&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;lock_expire&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">300.0</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Parameter</th><th>Type</th><th>Description</th><th>Default</th></tr></thead><tbody><tr><td><code>lock_timeout</code></td><td>float</td><td>Lock acquisition timeout (seconds). <code>0</code> = fail immediately if locked (default). <code>&gt; 0</code> = wait/retry up to this many seconds.</td><td><code>0.0</code></td></tr><tr><td><code>lock_expire</code></td><td>float</td><td>Lock inactivity threshold (seconds). Locks not refreshed within this window are treated as stale and reclaimed.</td><td><code>300.0</code></td></tr></tbody></table><h3 id="queuefs-persistence" tabindex="-1">QueueFS Persistence <a class="header-anchor" href="#queuefs-persistence" aria-label="Permalink to &quot;QueueFS Persistence&quot;">​</a></h3><p>The lock mechanism relies on QueueFS using the SQLite backend to ensure enqueued tasks survive process restarts. This is the default configuration and requires no manual setup.</p><h2 id="related-documentation" tabindex="-1">Related Documentation <a class="header-anchor" href="#related-documentation" aria-label="Permalink to &quot;Related Documentation&quot;">​</a></h2><ul><li><a href="./01-architecture">Architecture</a> - System architecture overview</li><li><a href="./05-storage">Storage</a> - AGFS and vector store</li><li><a href="./08-session">Session Management</a> - Session and memory management</li><li><a href="./../guides/01-configuration">Configuration</a> - Configuration reference</li></ul>`,88)])])}const u=n(i,[["render",o]]);export{k as __pageData,u as default};
